# ESP32-S3 桌面控制台：开发记录

## 2026-08-16：工程启动

### 已完成

- 安装并验证 ESP-IDF v5.5.5 完整离线环境。
- 锁定 LVGL 9.5.0、esp_lvgl_adapter 0.5.3 和 GT911 触摸驱动 1.2.1。
- 核对微雪 4.3 与 4.3B 最新官方 LVGL 9 示例。
- 建立 `board` 适配层，提供 4.3/4.3B 编译时选型。
- 实现 800×480 RGB 屏、GT911 触摸与 CH422G 背光开关。
- 建立 LVGL 横屏界面骨架，包含六个日常页面、页面库、程序坞和左右滑动。
- 建立双 OTA 分区表，每个应用分区 5MB，并预留约 5.9MB 设备存储区。
- 完成第一次全量编译，主固件约 892KB。

### 当前决策

- 日常开发先使用现有 4.3 无后缀板。
- 4.3B 作为最终硬件，到货后独立验证板载外设与供电。
- 联调阶段临时允许无蓝牙时亮屏，便于检查显示和触摸。
- 蓝牙认证和心跳状态机完成后，生产配置将恢复为启动锁定、蓝牙断开立即熄屏。

### 下一步

1. 用 UART Type-C 连接现有 4.3 板。
2. 读取芯片型号、Flash 容量和设备 MAC，不改写 Flash。
3. 核对成功后烧录首版固件。
4. 检查屏幕方向、色彩、触摸坐标、滑动切页和背光开关。

## 2026-08-16：无开发板准备基线

### 已完成

- 建立 `app_model`，统一管理连接、天气、指数、系统、控制条、AI 和媒体状态。
- 建立 `privacy`，实现启动锁定、认证、心跳、断开、Mac 锁屏与睡眠状态转换。
- 心跳周期设计为 2 秒，超时阈值为 6 秒；超时动作包含关背光、禁用触摸和清理私有状态。
- 建立 `ble_protocol`，实现版本化帧、消息编号、最大 512 字节负载和 CRC-16/CCITT-FALSE 校验。
- 完成蓝牙配对、应用层挑战认证、GATT 分片和业务字段协议草案。
- 页面改为从统一状态模型读取数据，加入中文首页、六指数、系统、控制条、AI、媒体和页面库。
- 启用 LVGL 内置 Source Han Sans SC 16px 中文字体；大号数字继续使用 Montserrat。
- 增加主机侧测试，覆盖隐私正常流程、认证失败、心跳超时、CRC、帧篡改和私有数据清理。
- 增加接板联调清单和一键编译前检查脚本。

### 验证结果

- 主机侧自动测试通过。
- ESP-IDF 全量编译通过，无需开发板参与。
- 固件镜像约 1.14MB，5MB 应用分区剩余 78%。
- DIRAM 使用约 143KB，占可用容量 41.87%。
- 当前未连接串口，未读取设备信息，也未执行烧录。

### 容量结论

- 当前功能不需要 microSD 卡。
- 分区已预留约 5.9MB 内置存储，可用于设置、少量公共缓存和诊断记录。
- TF 卡留给将来的大量截屏、主题、照片、动画或长期日志；4.3B 到货后再验证卡槽。

### 后续开发边界

- 已完成隐私状态机和协议内核，实际 BLE GATT 服务仍需接入 ESP-IDF 蓝牙栈。
- 天气、预警和指数的数据结构已经确定，Wi-Fi 配网与正式接口适配待后续阶段实现。
- Mac 助手待阶段 E 开始，先完成系统状态和控制动作，再接入 Codex、Claude Code 与媒体状态。

## 2026-08-17：microSD 纳入正式方案

### 新决定

- 用户已经购买存储卡，后续将 microSD 作为日志、诊断导出、截屏和大型资源的主要存储位置。
- 不再以少量 Flash 占用作为功能取舍的主要依据，开发时优先保证界面响应、蓝牙心跳和长期运行性能。
- 板载 Flash 继续保存固件、启动资源、设置、绑定信息和少量关键故障记录，确保未插卡时仍可启动和运行核心功能。

### 实现要求

- 日志通过内存队列异步批量写卡，不在界面、触摸或蓝牙回调中同步写文件。
- 支持日志轮换、存储卡状态检测、拔卡降级、写满保护和故障后恢复。
- 日志过滤敏感字段，禁止保存 Wi-Fi 密码、绑定密钥、通知正文、AI 提示词与回答。

### 已完成实现

- 将 LVGL 状态更新改为局部控件刷新。天气、指数、系统、控制条、AI、媒体和连接状态更新时不再清空并重建整页。
- 预警条出现/消失以及私有页面有效状态切换时保留结构重建路径，避免持有已经删除的控件引用。
- 按官方示例确认 microSD 使用 GPIO 11/13/12 的独立 SPI 总线，SD 片选通过 CH422G 扩展口控制。
- 为 CH422G 只写输出寄存器增加统一影子值和互斥保护，背光开关与 SD 片选会保留其他外设位。
- 新增 `storage` 组件：自动挂载、挂载失败重试、32 项无等待日志队列、每 8 条或 2 秒批量刷新、2MB 文件轮转。
- 首次挂载自动建立 `logs`、`screenshots` 和 `assets` 目录；挂载失败时绝不自动格式化存储卡。
- 日志接口要求调用方标记公共/私有内容，私有内容在进入队列前替换为固定占位文本。
- 主机测试新增日志换行清理、隐私替换、容量边界和文件命名覆盖。

### 验证结果

- 主机侧协议、隐私和日志策略测试全部通过。
- ESP-IDF v5.5.5 全量编译通过。
- 固件镜像为 1,224,352 字节，5MB 应用分区剩余 77%。
- 静态 DIRAM 使用 154,475 字节，占 45.2%，尚余 187,285 字节；存储任务采用低优先级和有界队列，不会随日志量无限占用内存。
- 本轮没有连接、读取或烧录开发板；屏幕局部刷新、CH422G 新状态管理、microSD 挂载和热插拔仍需晚间实机验证。

## 2026-08-17：新版固件接板验证

### 设备识别

- 通过 UART Type-C 识别到 ESP32-S3 QFN56，芯片版本 v0.2，40MHz 晶振。
- 板载 PSRAM 为 8MB，板载 Flash 为 16MB，Flash eFuse 配置为四线模式和 3.3V。
- 使用串口 `/dev/cu.usbmodem5C371869291` 完成烧录；设备唯一 MAC 未写入仓库文档。

### 烧录与启动结果

- 引导程序、分区表、OTA 初始数据和 1,224,352 字节应用镜像全部写入成功，哈希校验通过。
- 启动后确认 Flash 进入 QIO 80MHz，PSRAM 进入 120MHz，8MB PSRAM 内存测试通过。
- 800×480 RGB 面板、LVGL 任务和 GT911 触摸驱动初始化成功；GT911 识别码为 `911`，配置版本 67。
- 当前仍为联调亮屏配置，蓝牙隐私闭环完成前允许无认证亮屏。
- 未插入存储卡时，挂载返回 `ESP_ERR_TIMEOUT`，存储任务按 10 秒周期重试；连续观察两轮没有重启，主界面任务保持运行。

### 待人工与存储卡确认

- 人工确认屏幕方向、颜色、局部刷新、触摸坐标和左右滑动。
- 存储卡到货后验证 FAT32 挂载、目录创建、日志落盘、背光与 SD 片选互不影响，以及拔插恢复。

## 2026-08-17：完整中文后备字库与滑动修复

### 字体方案

- 用思源黑体生成项目专用 16px 字体，固定界面中的 179 个汉字保留 4 位灰度，保证常用页面文字清晰。
- 增加 2 位灰度压缩后备字库，覆盖中文基本区、扩展 A、中文标点、全角字符、日文假名、韩文、常用拉丁扩展和符号。
- 所有界面字体自动链接后备字库，蓝牙传入的歌名、应用名等动态文本无需预先登记汉字。
- 增加字体生成脚本和构建前覆盖检查。新增固定文案出现未收录字形时，检查会失败并提示重新生成。
- 保留思源黑体开放字体许可证。存储卡到货后可将完整原始字体放入资源目录，用于更大字号和更多 Unicode 扩展字符。

### 触摸修复

- 为内容区全部子控件开启手势冒泡，手指从标签、按钮或图表区域起滑时也能触发页面切换。
- 清除内容区自身默认携带的手势冒泡标志，使子控件上产生的手势最终停在内容区；此前事件会继续越过内容区到达根屏幕，导致内容区回调没有执行。
- 横向手势最小距离从 120 调整为 80，并设置最低速度阈值，提升 4.3 英寸横屏上的切页成功率。

### 验证结果

- 主机侧协议、隐私、日志和字体覆盖检查全部通过。
- 完整固件为 3,842,352 字节，每个 5MB OTA 应用分区剩余 27%。
- 已通过 UART 完成烧录和哈希校验；屏幕、GT911 触摸、8MB PSRAM 与 LVGL 均成功启动。
- 字体只读数据按当前高性能配置映射到 PSRAM 后，系统仍向动态堆提供约 4.48MB PSRAM，启动过程未出现内存分配失败。
- 未插存储卡时仍按 10 秒周期重试挂载，连续运行未发生崩溃或重启。
- 中文实际显示效果和左右滑动仍需用户在屏幕上最终确认。

## 2026-08-17：蓝牙传输基线与隐私监督接入

### 已完成实现

- 新增 `ble_link` 组件，使用 NimBLE 建立单 Mac 低功耗蓝牙外设，设备名为 `Desk Console 4.3`。
- 实装协议规定的一个 128 位主服务和三条特征：Mac 写入、设备事件通知、设备状态读取/通知。
- 启用 Secure Connections、绑定和 NVS 绑定信息保存，关闭传统配对协议；控制器只保留一个广播活动和一个连接活动。
- 首选 MTU 设为 247，同时保留 MTU 23 下的分片能力。
- 完成 4 字节 GATT 分片头、顺序检查、单帧重组、2 秒超时、512 字节负载上限和逻辑帧 CRC 复核。
- 蓝牙回调只写入固定长度事件队列，隐私监督任务以 250ms 周期消费事件并检查心跳，避免在蓝牙栈回调中刷新界面或执行存储操作。
- 连接、加密状态变化、断开、Mac 锁定和心跳事件已接到隐私状态机；锁定动作会清除系统、控制条、AI 和媒体数据，并支持关闭背光和禁用触摸。
- 增加 30 秒应用层认证等待上限，以及蓝牙启动失败时保留屏幕运行的降级路径。

### 实机验证

- 字体覆盖、协议、隐私和日志主机测试通过，ESP-IDF v5.5.5 完整编译通过。
- 当前固件镜像为 4,127,936 字节，每个 5MB OTA 应用分区剩余 21%。
- 已烧录开发板并确认屏幕、GT911、PSRAM、存储卡缺失降级和 NimBLE 主机均正常启动。
- 实机日志确认设备以 `Desk Console 4.3` 成功开始广播，启动后没有重启或蓝牙控制器错误。
- 初次实机检查发现通知特征缺少 NimBLE 要求的访问回调，以及控制器活动槽数量不足；两项均已修复。蓝牙启动失败现在不会再引起设备重启循环。

### 尚未完成

- `HELLO`、随机挑战、HMAC-SHA256 响应、首次共享密钥登记和 Mac 钥匙串保存仍待实现。
- Mac 助手尚未开始，因此暂时没有端到端验证配对、MTU 协商、应用认证、心跳和自动亮灭屏。
- 联调固件仍启用 `DESK_BRINGUP_DISPLAY_ON`，未认证时会保持亮屏和触摸可用；办公室版本必须在应用认证闭环验证后关闭该选项。

## 2026-08-17：macOS 蓝牙助手第一阶段

### 已完成实现

- 新建原生 macOS 菜单栏助手，使用 CoreBluetooth 主动扫描桌面控制台专用服务，无需在系统蓝牙设置中手动查找设备。
- 完成设备连接、服务发现、三条特征识别、事件与状态订阅，以及协议版本、MTU、加密、绑定和接收错误计数解析。
- 连接失败或断开后按 1、2、4、8 秒退避，后续每 15 秒重新扫描；菜单中提供立即重连和退出操作。
- 助手以正式 `.app` 包运行，包含蓝牙用途说明和菜单栏后台应用配置，首次启动由 macOS 请求一次蓝牙权限。
- 增加独立构建脚本和使用说明，Swift Package 的生成物已经加入忽略列表。

### 验证结果

- 使用 Apple Swift 6.2.3 完成 Debug 构建和临时签名，生成的菜单栏应用可以正常启动并保持运行。
- 当前 macOS 蓝牙权限请求仍在等待用户确认，因此扫描、连接和 ESP32 首页状态联动需要授权后继续观察。
- 固件应用层认证尚未完成，现有连接会在 30 秒认证等待上限到达后主动断开；助手会自动重连。这一现象会在挑战认证和心跳接入后消失。

## 2026-08-17：应用认证与心跳闭环

### 已完成实现

- 新增固件 `app_auth` 组件，使用设备随机数、Mac 随机数、会话编号和域分隔字符串计算 HMAC-SHA256。
- 首次联调时由 ESP32 生成 32 字节共享密钥，经已加密的 BLE 链路交给 Mac；ESP32 保存到 NVS，Mac 保存到系统钥匙串。
- 固件只在 HMAC 验证成功后设置 Mac 可信状态、启用触摸和触发亮屏动作；无效请求、密钥缺失、登记窗口关闭、保存失败和 HMAC 错误均有独立结果码。
- Mac 助手增加逻辑帧编解码、CRC、双向 GATT 分片、首次登记、随机挑战响应和 2 秒心跳。
- 助手监听 Mac 会话锁定、屏幕休眠和整机睡眠，先发送 `LOCK` 再断开连接；会话恢复后重新连接。
- 心跳超过 6 秒后，固件清理私有状态并主动断开 BLE，确保助手失去响应时也能回到安全状态。
- 开发应用包改用固定标识的指定签名规则，减少后续源码重编译造成的蓝牙权限失效；正式发布仍需 Apple 开发者签名。

### 编译与烧录

- 字体覆盖、主机侧协议/隐私/日志测试和 ESP-IDF 全量编译通过。
- Swift Debug 构建通过；完整固件为 4,137,808 字节，5MB 应用分区剩余 21%。
- 新固件已写入开发板，哈希校验通过；启动日志确认 NVS、屏幕、触摸、PSRAM、蓝牙广播和未插卡降级均正常。

### 当前联调点

- macOS 权限日志已经确认固定应用标识可沿用蓝牙授权，后续普通重编译不再触发重复授权。
- 发现 Mac 与开发板保留的旧低功耗蓝牙绑定状态不一致。macOS 将目标设备临时加入连接阻止列表，并报告设备已不再配对；需要在系统蓝牙设置中忽略一次 `Desk Console 4.3`，再建立新绑定。
- 助手已增加 10 秒连接超时，并将重连退避的清零时机移到应用认证成功后，避免旧绑定故障造成快速循环。开发联调固件收到重复配对请求时会删除开发板端旧绑定并重试。
- 新固件再次完成全量编译、写入和哈希校验。清理旧绑定后继续验证密钥登记、认证成功日志和持续心跳。
- 当前联调配置允许首台 Mac 自动登记密钥。办公室配置会关闭该入口，并增加设备端人工确认窗口。

### 绑定重启复核

- 清理旧绑定后，macOS 完成 Just Works 配对，链路加密与绑定均成功，MTU 协商为 247。
- 首次应用认证成功并登记共享密钥；助手进程重启后再次认证显示 `enrolled=0`，证明 Mac 钥匙串和开发板应用密钥均可复用。
- 设备重启复核发现 NimBLE 的 `BT_NIMBLE_NVS_PERSIST` 默认关闭，系统蓝牙绑定密钥此前只保存在内存中。项目默认配置和当前构建现已启用该选项，确保绑定密钥写入 NVS。
- 增加当前连接句柄校验，忽略旧连接遗留的加密状态事件；若连接到达时链路已经加密，则跳过重复安全协商。
- 持久化固件已经完成全量编译、烧录和哈希校验。需要最后清理一次 Mac 侧旧绑定并重新配对，然后执行开发板重启复测。

## 2026-08-17：系统实时状态与控制通道

### 已完成实现

- Mac 助手使用系统原生接口采集 CPU、内存、根卷剩余空间、网络上下行速率和电量，每 2 秒通过 `SYSTEM_STATE` 发送。
- 固件严格校验 JSON 边界、必填整数和取值范围，应用认证前拒绝系统数据，通过后局部刷新系统页。
- Mac 助手检测当前前台应用，只在应用变化时发送 `CONTROL_LAYOUT`，控制页标题会同步显示应用名。
- 控制页六个按钮已接入固定长度事件队列，由隐私监督任务在已认证状态下发送 `ACTION_TRIGGER`。未认证时按钮自动禁用。
- Mac 已实现后退、前进、刷新、新标签、系统截屏和打开终端。键盘类动作未获得辅助功能权限时静默拒绝，不主动请求新权限。

### 验证结果

- Swift 助手编译、应用打包和固定标识签名通过。
- 主机侧协议、隐私和日志测试通过；完整固件编译通过。
- 固件镜像为 4,155,392 字节，5MB 应用分区剩余 21%。
- 新固件已写入开发板并通过哈希校验；启动日志确认屏幕、触摸、8MB PSRAM、蓝牙广播和未插存储卡降级均正常。

## 2026-08-18：公共联网、真实数据与设备诊断

### 公共网络数据

- 新增 Wi-Fi Station 组件，沿用 ESP-IDF 的 NVS 凭据保存，支持已认证蓝牙配网、断线重试、IP/RSSI 状态和阿里云 NTP。
- 新增统一 HTTPS 组件，启用系统证书包校验、响应长度上限、超时、PSRAM 缓冲和全局串行握手，避免天气与行情同时建立 TLS 造成内存峰值。
- 天气组件使用和风天气当前坐标 API，读取当前状态、未来 6 小时、今明两天和有效预警；接口失败时保留最近一次有效状态。实现依据：[实时天气](https://dev.qweather.com/en/docs/api/weather/weather-current/)、[逐小时预报](https://dev.qweather.com/en/docs/api/weather/weather-hourly-forecast/)、[每日预报](https://dev.qweather.com/en/docs/api/weather/weather-daily-forecast/)、[天气预警](https://dev.qweather.com/en/docs/api/warning/weather-alert/)。
- 行情组件直接读取上交所与深交所公开接口，固定展示上证综指、深证成指、创业板指、科创 50、沪深 300 和中证 500。深交所分钟曲线响应采用有界字段提取，避免收盘后大数组进入 cJSON 对象树。官方页面：[上交所指数行情](https://www.sse.com.cn/market/sseindex/quotation/index.shtml)、[深交所指数行情](https://www.szse.cn/market/trend/index.html?code=399001)。
- OTA 应用分区从 5MB 扩大到 6MB，保留双 OTA；板载 SPIFFS 调整为约 3.9MB，大型资源、长期日志和截屏继续使用 microSD。

### 界面与诊断

- 系统页加入 CPU 与内存真实 60 秒曲线，每项只保留 30 个 16 位采样点，图表只在系统页创建。
- 页面库中的“设置”和“诊断”已经可打开。设置页汇总网络、蓝牙安全、公共数据与隐私策略；诊断页每 5 秒显示固件、开发板、运行时间、内部内存、最大连续块、PSRAM、存储卡与日志计数。
- 新增设备 IP 地址、固件版本和诊断状态模型。私密状态仍在蓝牙断开时清除，公共天气、行情、网络与设备诊断状态保留。
- 完整东亚后备字库继续保留，并重新生成 239 个高质量固定界面汉字，新增页面不会出现缺字方框。

## 2026-08-18：媒体与 AI 状态首版

- 媒体页接入上一首、播放/暂停、下一首、静音、音量降低和音量升高六个系统媒体键；助手通过 CoreAudio 读取当前输出音量。播放器歌名与进度暂未接入，当前使用通用标题。
- AI 协议加入额度可用性、额度窗口分钟数、任务数量和阶段。固件严格校验两个固定提供方及全部字段，断连时整页状态清除。
- Codex 助手通过官方 App Server 的 `account/rateLimits/read` 读取真实额度窗口，仅统计最近更新的会话文件数量，不读取任务名称、提示词、回复或项目路径。依据：[Codex App Server](https://developers.openai.com/codex/app-server/)。
- Claude Code 任务数通过本地进程统计。项目增加可选状态行采集脚本，可接收官方 `rate_limits.five_hour` 和 `rate_limits.seven_day` 字段；未自动修改用户配置。依据：[Claude Code 状态行](https://code.claude.com/docs/en/statusline)。
- Mac 助手的 Wi-Fi、天气、媒体与 AI 改动均已通过 Swift 编译和应用打包。固件主机测试已经覆盖天气与交易所响应解析；所有今晚代码尚未烧录，留待设备重新连接后逐项联调。
- 最终准备检查通过：字体覆盖、主机测试、ESP-IDF 全量编译和分区检查均成功。固件镜像为 4,847,040 字节，6MB OTA 分区剩余 23%。

## 2026-08-19/20：4.3B 实机联调、内存根因修复与真实数据接入

> 本阶段记录 4.3B（最终目标板，MAC `44:1b:f6:ca:4d:44`）的首次完整联调：从"开机重启循环、屏幕闪烁"到"认证稳定在线、行情真实数据上屏"。修复的多数问题在第二轮审查中已被标记为"待实机验证"，本次逐一落地。

### 硬件验证（4.3B）

- 屏幕（800×480 RGB）、GT911 触摸、8MB PSRAM（120MHz）、microSD、NimBLE 蓝牙全部工作。
- 引脚与 4.3 无后缀版完全一致（对照官方文档逐项核对）：LCD 16 位数据线、HSYNC 46 / VSYNC 3 / DE 5 / PCLK 7、触摸 SDA 8 / SCL 9 / IRQ 4、CH422G EXIO1=TP_RST / EXIO2=背光 / EXIO4=SD_CS、TF 卡 SPI 11/13/12。
- microSD 挂载成功（`microSD mounted at /sdcard`），CH422G 影子寄存器 + 独立 SPI 总线设计成立。

### 固件修复（按根因）

1. **BLE 控制器内存不足 → 开机重启循环**（屏幕上"顶部栏铺满+闪烁"的根因）
   - 现象：`BLE_INIT: Malloc failed` + `BLE assert emi.c 164, param 0x7800` + `RTC_SW_CPU_RST`，每 ~2 秒重启。
   - 根因：Wi-Fi + BLE + LVGL 挤爆内部 RAM；BT 控制器需要 **30KB（0x7800）连续内部 DMA 块**，而堆中最大连续块仅 28KB（差 2KB）。与 ESP-IDF 已知问题 [IDFGH-11255/Issue #12414](https://github.com/espressif/esp-idf/issues/12414) 同族。
   - 修复（`sdkconfig.defaults` + `board.c`，已提交 `1382dfd`）：
     - `CONFIG_BT_NIMBLE_MEM_ALLOC_MODE_EXTERNAL=y`（NimBLE 主机 → PSRAM）；
     - `CONFIG_SPIRAM_MALLOC_ALWAYSINTERNAL=64→16`；
     - Wi-Fi 静态接收缓冲 10→6、动态 32→16；
     - LCD 弹跳缓冲 `LCD_BOUNCE_LINES 10→4`（释放 9.6KB 连续内部内存）；
     - 默认板型切到 4.3B。
   - 120MHz Flash/PSRAM 实测稳定（根因是内存不是频率；官方 80MHz 是保守默认）。
2. **BLE 认证循环**（认证成功后 ~450ms 被助手断开）
   - 根因 A：`ble_link` 的 CONNECT 回调冗余重置 `link_state.subscribed`——NimBLE 在绑定重连时可能先派发 GATT 订阅事件、后派发连接完成事件，重置会抹掉订阅状态，导致认证挑战通知被 `Send blocked: subscribed=0` 阻塞（已提交 `41ee6dc`）。
   - 根因 B（Mac 侧）：`sendMessage` 用 `maximumWriteValueLength(for: .withResponse)` 计算分片（macOS 返回 512），AI_STATE 大帧单包超设备 244 上限，被 `CBATTErrorInvalidAttributeValueLength` 拒收 → 助手误判认证失败断开。修复：分片上限收敛到协商 MTU（244）。
3. **TLS 全部连接失败**（天气/行情 `create_ssl_handle failed`）
   - 根因：`CONFIG_MBEDTLS_INTERNAL_MEM_ALLOC=y`，TLS 握手缓冲（16KB 入 + 4KB 出 + 证书结构）挤占内部 RAM，分配失败。
   - 修复：`CONFIG_MBEDTLS_EXTERNAL_MEM_ALLOC=y`（TLS → PSRAM）。⚠️ **尚未写入 `sdkconfig.defaults`**，fresh clone 会退化回 internal。
4. **天气响应 gzip 无法解析**（`ESP_ERR_INVALID_RESPONSE`）
   - 根因：用户的和风 key 绑定专属 CDN host `ka5g7f299x.re.qweatherapi.com`，该 CDN **无条件返回 gzip**（`Accept-Encoding: identity` 无效，v1/v7 接口均如此），固件不解压 → 解析器拿到二进制。
   - 修复：`http_fetch` 增加 gzip 检测与解压（`esp_rom` ROM 内置 miniz，`tinfl_decompress_mem_to_mem` → PSRAM 缓冲，10 字节 gzip 头 + deflate + 8 字节尾）。⚠️ **编译通过，尚未烧录验证**。

### macOS 助手

- **稳定代码签名**：创建自签名证书 `Desk Console Development` 并写入 `build-app.sh`（缺失自动创建，私钥不进仓库）。根治 ad-hoc 签名每次重建都变 → 钥匙串 ACL 失效（`errSecInteractionNotAllowed -128`）→ 删除密钥 + 擦 NVS + 忘记设备的连锁循环。验证：连续两次构建签名身份一致。
- **控制台窗口**（菜单栏 → 打开控制台… / ⌘O）：蓝牙手动连接/断开、多 Wi-Fi 配置列表（添加/删除/下发所选）、天气配置（Host/Key/经纬度）。已修复配置列表在代码创建窗口下不加载的问题（`windowDidLoad` 对代码窗口不可靠，改为每次打开时加载）。
- **配置持久化**：Wi-Fi 凭据与天气配置保存在本机 UserDefaults（不进 git），控制台自动预填；设备侧配置写入 NVS 永久生效。
- ⚠️ **钥匙串已知项**：认证密钥由早期 ad-hoc 签名构建登记，ACL 仍绑定旧签名，稳定签名构建访问时 macOS 会弹授权框——可在弹窗点"始终允许"，或做一次干净重登记（删 Mac 钥匙串项 + 擦设备 NVS + 重配对）。

### 数据接入状态

- **行情（上交所 + 深交所）**：✅ 真实数据上屏（TLS 修复后 SSE 32042 端口正常，之前的"端口被拦"判断错误，实为 TLS 内存问题）。
- **天气**：host 已切到 `ka5g7f299x.re.qweatherapi.com`（key 只授权此 host，devapi/api 均 403 Invalid Host）；v1 坐标接口返回真实数据；**gzip 解压固件待烧录验证**。
- 设备开机即自动连网（`saved network=1`）+ 认证（稳定签名），插电即用符合设计目标。

### 遗留事项（下次联调）

1. 烧录 gzip 解压固件并验证天气首页显示真实数据（当前构建已包含，未上板）。
2. 把 `CONFIG_MBEDTLS_EXTERNAL_MEM_ALLOC=y` 写入 `sdkconfig.defaults`。
3. 钥匙串重登记（可选，或弹窗点"始终允许"）。
4. 移除 `weather.c` 中临时的响应调试日志（`Weather response (%u bytes)`）。
5. 内存分配规则已沉淀到《技术架构》"内部/PSRAM 内存预算"一节，新组件按表核对。

## 2026-08-20：全面代码审查 + 字库外置到 SD + 稳定性加固

> 本阶段先对全库做了一次全面审查（并逐条实机验证修复），再把 CJK 后备字库外置到 SD、
> 释放应用分区，最后修掉字体显示缺陷、去掉开机 mock、打开任务看门狗。应用镜像
> **4.62MB → 1.96MB**（6MB 分区剩余 23% → 67%）。以下所有改动均已实机（4.3B）验证。

### 全面审查后的修复（按严重度）

- **gzip 解压两处内存破坏（严重，`http_fetch.c`）**：`tinfl_decompress_mem_to_mem` 失败返回 `(size_t)-1` 而非 `0`，旧判据 `written==0` 漏判 → `context.length=SIZE_MAX` → `buffer[SIZE_MAX]=0` 堆越界；输出缓冲少分配 1 字节的 off-by-one。重写为独立 `inflate_gzip`：解压器（~11KB 霍夫曼表）分配到 **PSRAM**（避免爆 12KB 任务栈）、流式 `tinfl_decompress` + `TINFL_STATUS_DONE` 正确判据、输出缓冲 `+1`、按 gzip FLG 解析可选头（不再假设头恒 10 字节）、`Accept-Encoding: identity`→`gzip`。
- **`sdkconfig.defaults` 补 `CONFIG_MBEDTLS_EXTERNAL_MEM_ALLOC=y`**（遗留#2）：否则 fullclean/clone 后 TLS 退回 internal 分配再次崩溃。
- **NVS 初始化崩溃循环（中，`app_main.c`）**：`nvs_flash_init` 遇 `NO_FREE_PAGES/NEW_VERSION` 时 erase 后重试，避免首刷/改分区/OTA 开机 abort 循环。
- **supervisor 栈加固（中，`app_main.c`）**：栈 7168→8192、`encoded[524]` 移出栈改静态、解析不可信 payload 前限制 JSON 嵌套深度 ≤16（挡已认证对端深嵌套栈溢出）。
- **网络重连（中，`network.c`）**：改指数退避（1→2→…→60s 封顶）、**耗尽后不再永久放弃**（路由器重启后自动接回）、`state_lock` 保护共享状态、修配网 disconnect 的 TOCTOU（先置 pending 再断开）。
- **macOS 助手（中，Swift）**：`stateChanged` 单回调改多观察者 `observeState`（打开控制台不再冻结菜单栏状态）；控制台"连接"按钮改 `reconnectNow()`（断开后能真正重连）；Wi-Fi 密码 + 天气 API Key 迁入钥匙串（新增 `DeskSecretStore`，含旧明文一次性迁移）。
- **AI 页中文框框（`ui.c`）**：额度标签误用 `lv_font_montserrat_20/18`（纯拉丁无 fallback）渲染中文 → 方块；改回带 CJK fallback 的 `desk_ui_font_16`。
- **低危**：`weather.c` 响应调试日志 WARN→LOGD（遗留#4）。

### CJK 后备字库外置到 SD（应用镜像省 2.66MB）

- 字库（`desk_ui_cjk_font_16`）占旧镜像 2.77MB（.rodata 57%）。生成为 LVGL 二进制 `.bin`（2.29MB，`--format bin`），部署到 SD `/sdcard/desk_console/assets/`，从固件移除。
- LVGL 配置：`LV_USE_CLIB_MALLOC`（内置 64KB 池装不下 2.3MB 字体，切 CLIB 让大块落 PSRAM）、`LV_USE_FS_STDIO`（盘符 'S' 读 `/sdcard`）、`LV_USE_ASSERT_MALLOC=n`（加载 OOM 优雅返回 NULL 不卡死）。
- 固定字体 `desk_ui_font_16` 去掉编译期 fallback、改为**非 const**，运行时接 SD 字体（`generate-ui-font.sh` 同步：CJK→`.bin`、去 `--lv-fallback`、sed 去 const）。
- 加载放**后台低优先级任务、锁外执行**：`lv_binfont_create` 逐字节解析 ~2.3MB 需 ~33s，若持锁会阻塞联网/UI；仅在设置 `fallback` 一瞬间加锁。启动/联网恢复 ~3s，字库后台加载完成后动态中文自动补上。SD 缺失时固定界面正常、动态中文回退方块。
- **部署方式**：设备只有 USB-Serial/JTAG 连接（Mac 看不见 SD 磁盘），采用一次性 provisioning 固件——临时 `EMBED_FILES` 嵌入字库、开机写盘，写完回退。全程 USB、无需拔卡拷贝。

### 字体显示缺陷 + 开机体验 + 稳定性

- **压缩字体解码（根因，一直存在）**：CJK 字库为 2bpp 压缩（`lv_font_conv` 默认压缩），但未开 `LV_USE_FONT_COMPRESSED` → 后备字符解不出位图、显示空白（预警"新疆气象台…"里非固定字缺失成空格；每帧数百条 `Couldn't get the bitmap` 刷屏）。开启后位图告警归零，预警/动态中文完整显示。
- **去开机 mock**：`app_main` 用 `desk_app_state_init`（全 invalid）替代 `desk_app_state_load_mock`，公共数据初始显示"天气数据等待联网更新 / ----"，真实数据到达替换，不再先闪一屏假数据。
- **任务看门狗**：`ESP_TASK_WDT_EN=y` + `PANIC=y`（卡死 panic→自恢复重启而非硬挂）+ 超时 10s（覆盖字体加载/TLS 的 idle 尖峰，实测不误杀）。之前的自动重启在修好压缩字体（消除告警刷屏）后 180s 长抓未复现。

### 实机验证（4.3B）

- 应用镜像 1.96MB（6MB 分区剩 67%）；主机测试 + Swift 编译 + ESP-IDF 全量编译通过。
- 单次启动无重启/panic/OOM/WDT 误触发；BLE 1.9s、Wi-Fi 3s、认证 3.9s、行情/天气真实数据 7–12s、字库后台 ~35s 加载完成、TLS 持续握手正常；字体位图失败告警 0 条。

### 本轮新遗留

1. 动态中文开机首 ~30s 显示方块（字库后台加载中）；如需更快可整块读入 PSRAM 再解析（峰值内存换速度）。
2. SD 被格式化/字库被删 → 动态中文丢失，需重跑一次 provisioning 固件补写。
3. `.bin` 由 `generate-ui-font.sh` 生成、不入库（`assets/*.bin` 已 gitignore）。
