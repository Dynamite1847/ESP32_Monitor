# ESP32-S3 桌面控制台：开发环境

> 更新日期：2026-08-16

## 1. 当前环境

- macOS Apple Silicon
- ESP-IDF v5.5.5
- Python 3.13 独立虚拟环境
- CMake 4.4.2
- Ninja 1.13.2
- Xtensa ESP32-S3 工具链 14.2.0_20260121
- esptool.py v4.12.0
- LVGL 9.5.0
- esp_lvgl_adapter 0.5.3
- esp_lcd_touch_gt911 1.2.1

ESP-IDF 位置：

```text
/Users/dongyu/esp/v5.5.5/esp-idf
```

每次新建终端后先激活环境：

```bash
source '/Users/dongyu/.espressif/tools/activate_idf_v5.5.5.sh'
```

## 2. 开发板配置

| 配置 | 用途 | 当前状态 |
|---|---|---|
| `WAVESHARE_ESP32_S3_TOUCH_LCD_4_3` | 现有 4.3 无后缀开发板 | 默认，待连接后烧录验证 |
| `WAVESHARE_ESP32_S3_TOUCH_LCD_4_3_B` | 已购 4.3B 最终设备 | 官方参数已建档，待到货实机验证 |

两款板共用 ESP32-S3 N16R8、800×480 RGB 屏及主要显示和触摸引脚。工程仍保留独立的板级配置，新板到货后会核对背光、触摸复位、RTC、TF 卡和其他外设。

## 3. 下载与依赖策略

本项目默认优先使用官方完整包：

- 下载完整 ESP-IDF 离线归档，保留所有 ESP32-S3 工具链和组件缓存。
- 保留微雪官方完整示例、恢复固件、依赖、图片和硬件资料。
- 不为节省数 GB 空间主动裁剪官方包。
- 只有明确需要精简包时，才改用按需下载。

已保留的安装包：

```text
/Users/dongyu/Downloads/archive_v5.5.5_macos-aarch64-2.zst
/Users/dongyu/Downloads/eim
/Users/dongyu/Downloads/ESP32-S3-Touch-LCD-4.3-Demo/
```

## 4. 连接与烧录规则

- 开发和日志优先使用标有 `UART` 的 Type-C 接口。
- 第一次烧录前先读取芯片型号、Flash 容量和 MAC 地址。
- 开发板配置与实物不一致时停止烧录。
- 保留官方恢复固件，需要时可将开发板恢复到出厂演示状态。
- 日常修改先完成编译；只在开发板在线且型号已核对时烧录。

## 5. 当前待验证项

1. 连接 4.3 无后缀板，读取芯片与 Flash 信息。
2. 编译并烧录第一版固件，检查显示方向、色彩和触摸坐标。
3. 检查背光开启、关闭及锁定时触摸禁用。
4. 4.3B 到货后切换开发板配置，重复以上验证。

## 6. 首次编译结果

2026-08-16 已使用 4.3 无后缀默认配置完成全量编译：

| 项目 | 结果 |
|---|---:|
| 主固件 | 约 892KB |
| 单个 OTA 应用分区 | 5MB |
| 应用分区剩余 | 约 83% |
| 编译状态 | 通过 |
| 烧录状态 | 未执行，等待开发板连接 |

编译产物位于：

```text
/Users/dongyu/ClaudeCode/ESP32/firmware/build/esp32_desk_console.bin
```

当前工程已经具备 4.3 和 4.3B 编译时选型能力。4.3B 使用的屏幕时序、RGB 引脚、GT911 触摸连接和 CH422G 背光控制已与微雪官方 LVGL 9 示例对照。这只代表源码配置已核对，新板到货后仍需完成实机点亮和触摸验收。
